package com.bloom.bloomschool.students.util;

public enum Stage {
        APPLICATION_REVIEW,
        INTERVIEW_SCHEDULED,
        OFFER_SENT,
        FEE_PAYMENT,
        ENROLLED,
        REJECTED
}
